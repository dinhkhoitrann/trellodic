type WorkspacesViewProps = {
  onViewWorkspace: (_workspaceId: string) => void;
};

function WorkspacesView({ onViewWorkspace }: WorkspacesViewProps) {
  return <>111</>;
}

export default WorkspacesView;
