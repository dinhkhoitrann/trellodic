import Card from '@mui/material/Card';
// Instead of using compound components (E.x: Card.Actions, Card.Content, ...),
// we just import it as usual. This is because of making it work, even when using it with DnDKit lib

export default Card;
